\begin{table}[t]
  \centering
  \small
  \caption{BFC and SPECS on the regular suites (@n pairs). Mean and median are over the pairs an engine answered correctly; the counts cover every pair. @verdict}
  \label{tab:overview-regular}
  \begin{tabular}{lrrrrrrrr}
    \toprule
    Engine & Mean (ms) & Median (ms) & Correct & Unknown & Incorrect & Timeouts & Errors & OOM \\
    \midrule
@rows
    \bottomrule
  \end{tabular}
\end{table}
