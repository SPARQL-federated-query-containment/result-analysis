\begin{table}[t]
  \centering
  \small
  \caption{BFC and SPECS per operator on all suites (@n pairs). Correct is the number of pairs an engine answered correctly, and its median time is over those pairs. The average speedup of BFC over SPECS is the mean, over those pairs, of the mean time per run of SPECS divided by that of BFC, so a value above 1 means BFC is faster and below 1 means SPECS is faster; a dash means there is no such pair.}
  \label{tab:by-operator}
  \begin{tabular}{lrrrrrr}
    \toprule
    Operator & Pairs & BFC correct & SPECS correct & BFC median (ms) & SPECS median (ms) & \shortstack[r]{Average speedup\\BFC over SPECS} \\
    \midrule
@rows
    \bottomrule
  \end{tabular}
\end{table}
