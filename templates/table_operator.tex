\begin{table}[t]
  \centering
  \small
  \caption{Results of \solverName{} and SpeCS per mutation over all @n pairs.
Median times are over the pairs an engine answered correctly.
The speedup is the mean ratio of the time of SpeCS to that of \solverName{} over those pairs, so a value above 1 means \solverName{} is faster.
A dash marks a mutation with no such pair.}
  \label{fig:operator_bfc_specs}
  \begin{tabular}{lrrrrrr}
    \toprule
    Mutation & Pairs & \solverName{} correct & SpeCS correct & \solverName{} median (ms) & SpeCS median (ms) & \shortstack[r]{Average speedup\\\solverName{} over SpeCS} \\
    \midrule
@rows
    \bottomrule
  \end{tabular}
\end{table}
