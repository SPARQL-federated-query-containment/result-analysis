\begin{table}[t]
  \centering
  \small
  \caption{BFC and SPECS on all suites (@n pairs). Timings are over the pairs an engine answered correctly; the counts cover every pair. @verdict}
  \label{tab:overview-all}
  \begin{tabular}{lrrrrrrrr}
    \toprule
    Engine & Mean (ms) & Median (ms) & Correct & Unknown & Incorrect & Timeouts & Errors & OOM \\
    \midrule
@rows
    \bottomrule
  \end{tabular}
\end{table}
